---
  - name: remove default nvidia driver
    become: true
    pacman:
      name:
        - nvidia
      state: absent

  - name: install nvidia dkms driver
    become: true
    pacman:
      name:
        - nvidia-dkms
      state: present

